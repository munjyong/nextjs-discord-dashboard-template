/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['cdn.discordapp.com', 'react.semantic-ui.com'],
    },
};

module.exports = nextConfig;
